# Assumption Registry — VN2

All assumptions: **Owner** = Human — requires manual review. **Status** = Registered — not yet validated (unless noted).


| ID      | Category            | Assumption statement                                                                                                                                                           | Evidence basis                                                                       | Testable condition                                           | Failure consequence                               |
| ------- | ------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------------- |
| ASM-001 | Business Logic      | Shortage cost **cu = 1.0** and holding cost **co = 0.2** per unit per the evaluation logic are correct for the scenario being simulated.                                       | `README.md` competition details; `Costs` defaults in `scripts/full_L3_simulation.py` | Assert cu/co in sim match documented values for a golden run | Wrong benchmark comparisons                       |
| ASM-002 | Business Logic      | Critical fractile for newsvendor-style ordering is **≈ 0.833** (cu/(cu+co)).                                                                                                   | README; paper docs                                                                   | Check `costs_for_service_level` / SL schedule vs formula     | Systematic over/under-ordering                    |
| ASM-003 | Data Structure      | There are **599** store–product SKUs in the competition initial state and sales files used by the simulator.                                                                   | README; `full_L3_simulation` loads initial state CSV                                 | Row count of unique (Store, Product) in Week 0 state         | Partial coverage, wrong totals                    |
| ASM-004 | Data Structure      | Checkpoint quantiles are stored as pickles under `{model}/{store}_{product}/fold_{k}.pkl` with a `quantiles` object indexed by horizon.                                        | `model_eval.py`, `full_L3_simulation.py` `load_quantiles`                            | Programmatic load + schema check for sample SKU              | Eval/skip or fallback to `fold_0` / default model |
| ASM-005 | Data Quality        | Raw weekly sales CSVs exist under `data/raw` with expected naming (e.g. `Week N - … - Sales.csv`).                                                                             | `full_L3_simulation.py`, `compute_bias_analysis.py` week file maps                   | File existence check for weeks 1..max_weeks                  | Simulation abort or missing demand                |
| ASM-006 | Data Quality        | Demand / state parquet paths used by `go eval-models` exist when SIP evaluation is enabled (`demand_imputed.parquet` or capped variant; `state.parquet` for SIP).              | `cli.py` `cmd_eval_models`                                                           | Path existence before `run_evaluation`                       | FileNotFoundError                                 |
| ASM-007 | Environment         | Runtime Python **≥ 3.11** and dependency versions satisfy `pyproject.toml` ranges.                                                                                             | `pyproject.toml`                                                                     | `python --version`; `pip check` or uv lock                   | Import or numerical failures                      |
| ASM-008 | Performance         | Parallel eval (`joblib` / `n_jobs`) is permitted by the host OS (semaphores, process limits).                                                                                  | `model_eval.py` Parallel                                                             | Smoke eval with `n_jobs=1` vs `n_jobs>1`                     | PermissionError in sandboxed CI                   |
| ASM-009 | Business Logic      | **Lead time** implemented in the primary 8-week backtest (`full_L3_simulation`) is **L=3** (orders arrive start of week t+3), not necessarily the README’s “2 weeks” phrasing. | `full_L3_simulation.py` comments and logic                                           | Documented golden scenario vs README                         | Mis-stated competition fidelity                   |
| ASM-010 | Data Structure      | `eval_folds` parquet contains columns `store`, `product`, `model_name`, `fold_idx`, and cost columns such as `sip_realized_cost_w2` when using SIP path.                       | `model_selector.py`, `model_eval.py`                                                 | Schema assertion on read                                     | Selector or aggregate failures                    |
| ASM-011 | Business Logic      | Per-SKU selector requiring **8 folds** per model uses the **last N distinct fold indices** per SKU, not “N rows total per SKU” (fixed in `model_selector.py`).                 | `model_selector.py` `_in_last_n_folds`                                               | Unit/integration test on synthetic folds                     | Empty selector → default model for all SKUs       |
| ASM-012 | Data Quality        | `configs/forecast.yaml` **absolute paths** point to valid directories on the machine running training/backtests.                                                               | `configs/forecast.yaml`                                                              | Resolve paths exist                                          | Training writes to wrong place or fails           |
| ASM-013 | External Dependency | GluonTS / PyTorch / Lightning are compatible on the target OS (e.g. Mac Metal flags in config).                                                                                | `configs/forecast.yaml` `use_metal`                                                  | Train one DeepAR step                                        | OOM or backend errors                             |
| ASM-014 | Security            | No secrets are required for local batch runs; MLflow may be configured but not mandatory for core paths.                                                                       | Grep / docs — **unstated — inferred**                                                | Attempt run without MLflow server                            | Optional dependency failures                      |
| ASM-015 | Data Structure      | Selector map parquet has columns `store`, `product`, `model_name` for `full_L3_simulation` `load_selector_map`.                                                                | `full_L3_simulation.py`                                                              | Column check                                                 | Empty map or KeyError                             |
| ASM-016 | Temporal ordering   | Fold index **0..7** (or holdout convention) aligns with chronological week ordering assumed by `prepare_train_test_split` and competition weeks.                               | `model_eval.py` task generation                                                      | Compare fold_idx to calendar week labels                     | Temporal leakage or mis-ordered costs             |
| ASM-017 | Business Logic      | Integerization of orders/demand (ceil, round) in cost paths matches intended conservative behavior.                                                                            | `EVALUATION_V3_SUMMARY.md`, eval code                                                | Spot-check integer costs are multiples of 0.2 / 1.0          | Off-by-one cost accumulation                      |
| ASM-018 | Data Quality        | Imputed demand files (`demand_imputed.parquet`, `demand_imputed_capped.parquet`) are generated from the same raw universe as checkpoints.                                      | CLI defaults — **inferred**                                                          | Hash or row count consistency check                          | Train/eval distribution skew                      |
| ASM-019 | Key integrity       | `(store, product)` keys are consistent across initial state, sales, and checkpoints (same integer encoding).                                                                   | **Inferred from implementation**                                                     | Join test: state keys ⊆ checkpoint keys universe             | Silent default_model fallback                     |
| ASM-020 | Performance         | Batch size and task count for full eval (~models × SKUs × folds) is feasible on available RAM/time.                                                                            | `model_eval.py` generates tasks                                                      | Progress file + batch parquet sizes                          | OOM, partial eval                                 |
| ASM-021 | Business Logic      | Service-level schedule for orders (flat 0.833) uses `costs_for_service_level` consistently with eval costs separation.                                                         | `full_L3_simulation.py`                                                              | Compare ordering cu/co vs eval cu/co                         | Historical “cost tallying bug” class of errors    |
| ASM-022 | Environment         | `sysctl` / CPU detection in `go eval-models` works on the platform (or `--n-jobs` is set).                                                                                     | `cli.py` `cmd_eval_models`                                                           | Run without sysctl (e.g. Linux CI)                           | ValueError on empty sysctl output                 |


**Gaps (could not fully verify from static read):**

- Exact column sets for every parquet variant under `data/processed` without sample files in repo (often gitignored).
- Whether production MLflow tracking URI is required for any mandatory pipeline — **requires human input** if enforcing manifest-only runs.

